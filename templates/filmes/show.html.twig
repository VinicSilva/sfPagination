{% extends 'base.html.twig' %}

{% block title %}Filmes{% endblock %}

{% block body %}
    <h1>Filmes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filme.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ filme.titulo }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ filme.descricao }}</td>
            </tr>
            <tr>
                <th>Ano_lancamento</th>
                <td>{{ filme.anoLancamento }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('filmes_index') }}">back to list</a>

    <a href="{{ path('filmes_edit', {'id': filme.id}) }}">edit</a>

    {{ include('filmes/_delete_form.html.twig') }}
{% endblock %}